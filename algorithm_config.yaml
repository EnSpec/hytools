# DO NOT DELETE
# THIS CONFIG IS AUTO-GENERATED BY ADE UI
algo_name: hytools
version: system-test-3
environment: ubuntu
repository_url: https://gitlab.com/geospec/hytools.git
docker_url: registry.imgspec.org/root/ade_base_images/plant:latest

# fill out these fields
# explain what this algorithm does
description: Performs topographic correction and brdf correction
# path to the wrapper script for running the algorithm
run_command: hytools/.imgspec/imgspec_run.sh
# set a storage value in GB or MB or KB, e.g. "100GB", "20MB", "10KB"
disk_space: 50GB
inputs:
# remove below this line if no inputs
# rename and set algorithm input names accordingly
  - name: l1_granule
    download: true
  - name: l2_granule
    download: true
  - name: image_correct_config
    context: true
  - name: trait_estimate_config
    context: true